package ex2025ribltbft.riblt

import ex2025ribltbft.riblt.Mapping

class MappingTest extends munit.FunSuite:
    test("test mapping") {
      val m = Mapping("hello".getBytes)
      var i = 0
      while i < 100 do
          i = i + 1
          // print(s"$i: ")
          // print(m.nextIndex)
          // print("\n")

    }
